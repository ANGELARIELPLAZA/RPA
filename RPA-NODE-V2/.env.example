PORT=3000
HOST=0.0.0.0
NODE_ENV=production

USUARIO=
PASSWORD=

# Opcional: contraseÃ±as por agencia (cuando el payload incluye `agencia` o `agency`).
# 1) Mapa JSON (las llaves se normalizan a minÃºsculas):
# PASSWORDS_BY_AGENCIA={"001":"pass1","agencia_sur":"pass2"}
# 2) Variables individuales (se normaliza a PASSWORD_AGENCIA_<AGENCIA>):
# PASSWORD_AGENCIA_001=pass1
# PASSWORD_AGENCIA_AGENCIA_SUR=pass2
# Si es `true`, rechaza ejecuciÃ³n cuando viene `agencia` y no hay password configurado.
AGENCIA_PASSWORD_STRICT=false

HEADLESS=true
RECORD_VIDEO=false

CETELEM_URL=https://cck.creditoclick.com.mx/users-web/auth/kia/login?w=true
PING_TIMEOUT_MS=5000
STATUS_TIMEOUT_MS=4000

SCREENSHOTS_DIR=./screenshots
BASE_URL=http://localhost:3000

MONITOR_ENABLED=true
MONITOR_REFRESH_MS=1000

TRACKING_ENABLED=true
TRACKING_SERVICE_URL=http://rpa-tracking-service:3100
TRACKING_TIMEOUT_MS=3000
