services:
  rpa-node-v2:
    build: .
    container_name: rpa-node-v2
    ports:
      - "3000:3000"
    env_file:
      - .env
    volumes:
      - ./screenshots:/app/screenshots
      - ./logs:/app/logs
      - ./videos:/app/videos
    restart: unless-stopped

